<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.emjaay.mdb"
    android:versionCode="4"
    android:versionName="0.4 alpha" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name=".application.EmdbApplication" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize|stateVisible" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_home" >
        </activity>
        <activity
            android:name=".SearchImdbActivity"
            android:label="@string/title_search_movie" >
        </activity>
        <activity
            android:name=".MoviePickerActivity"
            android:label="@string/title_pick_movie" >
        </activity>
        <activity
            android:name=".DetailedMovieActivity"
            android:label="@string/title_detailed_movie" >
        </activity>
    </application>

</manifest>